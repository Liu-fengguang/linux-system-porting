# U-Boot 介绍

## 一、什么是 U-Boot

| 概念 | 说明 |
|------|------|
| 全称 | Universal Boot Loader（通用引导加载器） |
| 本质 | 一个比较复杂的**裸机程序** |
| 定位 | Bootloader，为启动 Linux 或其他系统做准备 |

### 为什么需要 U-Boot

Linux 运行在 DDR 里，而 Linux 镜像存储在 SD/EMMC/NAND/SPI FLASH 等外置存储中。

```
外置存储 (SD卡/EMMC/NAND)
  ├── zImage (Linux 内核镜像)
  └── .dtb    (设备树)
       │
   需要有人把它们拷进 DDR 并启动
       │
      U-Boot ← 干这个活的
```

### U-Boot 的主要工作

| 任务 | 说明 |
|------|------|
| 初始化 DDR | Linux 跑在 DDR 里，必须先把 DDR 配好 |
| 搬运镜像 | 把 zImage + dtb 从外置存储拷到 DDR |
| 启动内核 | 跳转到内核入口执行 |

### U-Boot 的能力范围

| 能启动什么 | 说明 |
|-----------|------|
| Linux | 主流用途 |
| VxWorks | 其他嵌入式系统 |
| 支持多架构 | ARM、MIPS、RISC-V、x86 等 |

> U-Boot 是通用 bootloader。Linux 也不只能靠 U-Boot 启动。

---

## 二、U-Boot 的三种来源

```
U-Boot 官网（主线版本）
  │  缺点：对某款具体芯片的驱动支持不完善
  ▼
SoC 厂商定制版（NXP 官方）
  │  NXP 下载主线某版本，加入 I.MX6ULL 驱动
  │  对应官方 EVK 评估板
  ▼
开发板厂商定制版（正点原子）
     官方 EVK 板子和自家板子不一样（DDR、引脚布局）
     在 SoC 版本基础上再改，适配自己的板子
```

| 层级 | 谁做的 | 改了什么 | 适合谁 |
|------|--------|----------|--------|
| ① 官方主线 | U-Boot 官网 | 原始版本 | 参考 |
| ② SoC 定制 | NXP | + I.MX6ULL 驱动 | NXP EVK 板 |
| ③ 板卡定制 | 正点原子等 | + 适配 Alpha 板 | 你手上的板子 |

> 学习移植时，我们拿的是第 ② 或第 ③ 层版本，在它的基础上改成自己板子需要的配置。
